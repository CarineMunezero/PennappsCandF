import React from 'react';

const Home = () => {
    const textStyle = {
        fontSize: '70px', // Adjust the size as needed
        textAlign: 'center', // Adjust the alignment as needed ('left', 'center', 'right', 'justify')
      };
  return (
    <div>
      <h1 style={textStyle}>Test Your Knowledge</h1>
    </div>
  );
};

export default Home;